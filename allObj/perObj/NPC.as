package  allObj.perObj{
	import flash.display.MovieClip;
	public class NPC extends MovieClip{
		var questlist:Array = [];
		var talkNumber:int;
		public function NPC(dN:int) {
			talkNumber = dN;
		}
		/*public function addQuest(qList:Array){
			if(qList[0] == "if"
		}
*/
	}
	
}
